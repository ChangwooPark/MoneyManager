import AppShell from '@/components/AppShell';

export default function Home() {
  return (
    <AppShell>
      {/* Phase 8 이후 메인 화면이 여기 들어옴 */}
      <div className="flex items-center justify-center h-full">
        <p style={{ color: 'var(--text-secondary)' }}>메인 화면 준비 중...</p>
      </div>
    </AppShell>
  );
}
